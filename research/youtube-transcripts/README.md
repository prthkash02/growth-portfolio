# YouTube Transcripts

Transcripts pulled from the experts' videos/talks, organized one folder per author, one file per video.

Collection method: `youtube-transcript-api` (Python) for auto-captions, falling back to `yt-dlp --write-auto-sub` where captions are disabled. Each file keeps the video title, URL, and date at the top.

> Filling this in tomorrow.
